#include <stdio.h>

int main() {
    int Arr[5] = {10, 20, 30, 40, 50};
    int i;
    printf("Elements of the array are:\n");
    for (i = 0; i < 5; i++) {
        printf("Arr[%d] = %d\n", i, Arr[i]); // Added '\n' for a line break
    }
    return 0;
}
